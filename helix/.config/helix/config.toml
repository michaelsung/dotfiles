theme = "catppuccin_mocha"

[editor]
line-number = "relative"
cursorline = true
color-modes = true
bufferline = "always"
auto-format = true

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[editor.file-picker]
hidden = false

[editor.gutters.line-numbers]
min-width = 4

[editor.indent-guides]
render = false

[editor.lsp]
display-messages = true

[editor.soft-wrap]
enable = true

[editor.statusline]
left = ["mode", "spacer", "spacer", "version-control", "spacer", "spacer", "file-name"]
center = []
right = ["spinner", "diagnostics", "selections", "position-percentage", "file-encoding", "file-line-ending", "file-type"]
separator = "│"
mode.normal = "NORMAL"
mode.insert = "INSERT"
mode.select = "SELECT"

[keys.normal]
C-p = "file_picker"

# [[language]]
# name = "html"
# formatter = { command = 'prettier', args = ["--parser", "html"] }

# [[language]]
# name = "json"
# formatter = { command = 'prettier', args = ["--parser", "json"] }

# [[language]]
# name = "css"
# formatter = { command = 'prettier', args = ["--parser", "css"] }

# [[language]]
# name = "javascript"
# language-servers = ["eslint", "typescript-language-server"]
# formatter = { command = 'prettier', args = ["--parser", "typescript"] }
# auto-format = true

# [[language]]
# name = "typescript"
# language-servers = ["eslint", "typescript-language-server"]
# formatter = { command = 'prettier', args = ["--parser", "typescript"] }
# auto-format = true

# [[language]]
# name = "tsx"
# formatter = { command = 'prettier', args = ["--parser", "typescript"] }
# auto-format = true